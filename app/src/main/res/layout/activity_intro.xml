<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/itadaki_charcoal">

    <!-- Animated GIF background -->
    <ImageView
        android:id="@+id/steam_animation"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/itadaki_tagline" />

    <!-- Gradient overlay for readability -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_intro_gradient"
        android:alpha="0.7" />

    <!-- Content Container -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="32dp">

        <!-- Logo -->
        <ImageView
            android:id="@+id/itadaki_logo"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:src="@drawable/itadaki_logo"
            android:contentDescription="@string/itadaki_app_name"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/app_name"
            app:layout_constraintVertical_chainStyle="packed"
            android:layout_marginBottom="24dp"/>

        <!-- App Name -->
        <TextView
            android:id="@+id/app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/itadaki_app_name"
            android:textColor="@color/itadaki_white"
            android:textSize="48sp"
            android:fontFamily="@font/quicksand"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/itadaki_logo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/divider"
            android:layout_marginBottom="16dp"/>

        <!-- Divider -->
        <View
            android:id="@+id/divider"
            android:layout_width="100dp"
            android:layout_height="3dp"
            android:background="@color/itadaki_white"
            android:alpha="0.6"
            app:layout_constraintTop_toBottomOf="@id/app_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/itadaki_tagline"
            android:layout_marginBottom="16dp"/>

        <!-- Tagline -->
        <TextView
            android:id="@+id/itadaki_tagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/itadaki_tagline"
            android:textColor="@color/itadaki_white"
            android:textSize="18sp"
            android:fontFamily="@font/noto_sans_jp"
            android:letterSpacing="0.08"
            android:alpha="0.9"
            app:layout_constraintTop_toBottomOf="@id/divider"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/btn_get_started"
            android:layout_marginBottom="48dp"/>

        <!-- Get Started Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_get_started"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:text="@string/itadaki_get_started"
            android:textColor="@color/itadaki_white"
            android:textSize="16sp"
            android:textStyle="bold"
            android:fontFamily="@font/noto_sans_jp"
            android:letterSpacing="0.1"
            app:backgroundTint="@color/itadaki_red"
            app:cornerRadius="28dp"
            app:elevation="8dp"
            app:rippleColor="@color/itadaki_white"
            app:layout_constraintTop_toBottomOf="@id/itadaki_tagline"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintWidth_percent="0.7"
            android:layout_marginBottom="64dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>