package com.eightbitlab.com.blurview

import android.content.Context

/**
 * Placeholder class that mirrors the BlurView RenderScriptBlur api surface so existing
 * initialization code can stay untouched. The logic lives directly inside BlurView.
 */
class RenderScriptBlur(@Suppress("UNUSED_PARAMETER") context: Context)
